apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: conversation-anthropic
spec:
  type: conversation.anthropic
  version: v1
  metadata:
    - name: key
      secretKeyRef:
        name: anthropic-api-key
        key: key
