apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: conversation-openai
spec:
  type: conversation.openai
  version: v1
  metadata:
    - name: key
      secretKeyRef:
        name: openai-api-key
        key: key
